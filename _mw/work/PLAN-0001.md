# PLAN-0001 — MiniDoc 0.1 bounded realization, revision 2

**Status:** admitted for implementation after `INBOX-0002` reconciliation  
**Commission:** `WORK-0001`  
**Current Product Baseline:** absent; partial non-admitted source candidate exists  
**Target:** `TARGET-WHAT-0001` revision 2, `TARGET-HOW-0001` revision 2, `PA-0001` revision 2  
**Mutable scope:** Product source, build/install/verification scripts, Product/user engineering documentation, Evidence/Work Result owners in this repository  
**External effects:** repository mutations only; Windows install/runtime execution remains outside the agent environment unless a compatible execution surface is available

A separate Roadmap still adds no value: this is one greenfield Product contour. The second inbox changes the implementation delta but does not introduce a multi-release transition topology.

## Required input resolution

Cold resolver paths from `_mw/WORKSPACE.md` yield `WORK-0001`, both developed inputs, current `SCIENCE-0001`, current Target WHAT/HOW, `PA-0001`, Decisions, Questions and this Plan. Governing MADARAII revision remains `7d5ef3d92c4e0982061d422208fdf913c55cc604`.

## Current implementation slice

The existing non-admitted candidate already contains a .NET 10 WPF shell, paragraph/run DOCX codec, Windows PDF module scaffolding and explicit storage boundary. It was produced under the superseded target feature boundary. It is retained as implementation baseline rather than discarded.

Material deltas now required:

- replace the flat menu/toolbar UX with bounded WPF Ribbon organization;
- add structure-preserving Find/Replace/Replace All;
- extend direct formatting with highlight and bounded paragraph/cell fill/spacing/indent;
- extend compatibility scanner + FlowDocument reader/writer to simple editable tables;
- add table insertion and rectangular row/column operations;
- extend compatibility extraction for unsupported drawings/fields/footnotes with explicit preview/placeholder/reference text behavior where feasible;
- complete PDF/storage/build/install/tests/docs/evidence and verify the exact candidate.

## Implementation slices

| Slice | Outcome | Dependency | Producer evidence |
|---|---|---|---|
| `S1` shell revision | first-party Ribbon, mode-aware groups, quick access, commands | revised target | XAML/build inspection + UI Windows test |
| `S2` editor operations | find/replace, highlight/fill/paragraph commands, simple table insertion/mutation | S1 | algorithmic fixtures/static checks + Windows interaction test |
| `S3` DOCX codec revision | simple-table/format admission, read/write, compatibility extraction and preservation | S2 | generated/opened fixtures, output reclassification, payload preservation checks |
| `S4` PDF/storage | complete first-party PDF rendering, shutdown containment and explicit document writer | existing mechanisms + target | static/build checks; Windows runtime process test |
| `S5` lifecycle | repository-clean build; per-machine install/uninstall/ownership manifest | S1–S4 | script checks; Windows execution/residue test |
| `S6` docs/verification | user/build/compatibility docs, automated static/fixture checks and Windows verification script | S1–S5 | reproducible commands and captured results |
| `S7` fan-in | exact Product candidate, Implementation Result, typed verification, integration/state reconciliation and closure audit | all | repository revision + Evidence owners |

## Invariants

No third-party product/runtime dependency. No app-state/temp/cache writes. No silently lossy editable DOCX save path. Simple-table mutations remain rectangular. Unsupported drawing/field/reference semantics never enter editable-save authority. PDF stays read-only. User documents stay outside uninstall ownership. No background runtime component. Unsupported system claims remain explicitly pending Windows-local evidence.

## Choice delegation

Private helper names and local source arrangement are delegated while preserving `PA-0001`. User-visible feature semantics, platform/dependency boundary, compatibility admission, persistence ownership, save durability trade-off, process-exit behavior, object/reference compatibility boundary and installation ownership are fixed.

## Recovery and interruption

Every slice is repository-local and versioned. Before a material write, re-resolve current target/plan. A discovered design gap stops only the affected slice and returns it to Target HOW/WHAT. Partial source remains non-current until verification/integration. Any Windows-only claim remains an evidence gap rather than being inferred from source.

## Verification contract

Repository/static and DOCX algorithmic claims are verified to the maximum available depth. Windows-only claims are verified by `scripts/verify-windows.ps1` plus documented manual actions against the exact locally built candidate. Verdicts distinguish `CONFORMS`, `IMPLEMENTATION_DEFECT`, `DESIGN_GAP`, `EVIDENCE_GAP` and system-test dependency rather than forcing a global PASS.

# PLAN-0001 — MiniDoc 0.1 bounded realization

**Status:** admitted for implementation  
**Commission:** `WORK-0001`  
**Current Product Baseline:** absent  
**Target:** `TARGET-WHAT-0001`, `TARGET-HOW-0001`, `PA-0001`  
**Mutable scope:** Product source, project/build/install/verification scripts, Product/user engineering documentation, Evidence/Work Result owners in this repository  
**External effects:** repository mutations only; no Windows install/deploy execution in the agent environment

A separate Roadmap adds no value: this is one compact greenfield Product contour with one target and no migration/current Product topology.

## Required input resolution

Cold resolver paths from `_mw/WORKSPACE.md` yield `WORK-0001`, `SCIENCE-0001`, `TARGET-WHAT-0001`, `TARGET-HOW-0001`, `PA-0001`, Decisions, Questions, and this Plan. Governing MADARAII revision remains `7d5ef3d92c4e0982061d422208fdf913c55cc604`.

## Implementation slices

| Slice | Outcome | Dependency | Producer evidence |
|---|---|---|---|
| `S1` project shell | .NET 10 WPF project, no package references, compact GUI/session state | targets | project/source inspection |
| `S2` DOCX | bounded loader, scanner, editor mapping, serializer, package preservation, explicit writer | S1 | fixture/self-test logic + static review |
| `S3` PDF | Windows-native page rendering and process-lifecycle containment | S1 | source review; Windows runtime test pending by environment |
| `S4` lifecycle | repository-clean build; install/uninstall/ownership manifest | S1 | script static checks; Windows execution pending |
| `S5` docs/verification | user/build/compatibility docs and verification scripts | S1–S4 | documentation trace + checker outputs where executable |
| `S6` fan-in | exact Product candidate, Implementation Result, verification evidence, Product admission/closure state | all | repository candidate revision and typed verdicts |

## Invariants

No third-party product/runtime dependency. No app-state/temp/cache writes. No silently lossy DOCX editable-save path. PDF stays read-only. User documents stay outside uninstall ownership. No background runtime component. Unsupported system claims remain explicitly pending Windows-local evidence.

## Choice delegation

Source filenames and private helper shapes are delegated while preserving `PA-0001`. User-facing feature semantics, platform/dependency boundary, compatibility admission, persistence ownership, save durability trade-off, process-exit behavior, and install ownership are fixed by Target owners and Decisions.

## Recovery and interruption

Every slice is repository-local and versioned. Before a material write, re-resolve the current target/plan. A discovered design gap stops only the affected slice and returns it to Target HOW/WHAT. A partial repository candidate remains non-current until verification/integration.

## Verification contract

Repository/static and DOCX algorithmic claims should be verified in the available environment to the maximum possible depth. Windows-only claims are verified by `scripts/verify-windows.ps1` plus documented manual actions against the exact locally built candidate. The final verdict must distinguish `CONFORMS`, `EVIDENCE_GAP`, and implementation/design defects per claim rather than force a global green result.

# PLAN-0001 — MiniDoc 0.1 bounded realization, revision 2

**Status:** executed and terminal for `WORK-0001`; exact candidate admitted as `PRODUCT-0001`  
**Commission:** `WORK-0001`  
**Initial Product Baseline for this revision:** absent; partial non-admitted source candidate existed  
**Target:** `TARGET-WHAT-0001` revision 2, `TARGET-HOW-0001` revision 2, `PA-0001` revision 2  
**Mutable scope:** Product source, build/install/verification scripts, Product/user engineering documentation, Evidence/Work Result owners in this repository  
**External effects:** repository mutations plus bounded Windows verification effects executed and reconciled by `EV-0001`

A separate Roadmap added no value: this was one greenfield Product contour. The second inbox changed the implementation delta without introducing a multi-release transition topology.

## Required input resolution

Cold resolver paths from `_mw/WORKSPACE.md` yield `WORK-0001`, both developed inputs, current `SCIENCE-0001`, current Target WHAT/HOW, `PA-0001`, Decisions, Questions and this Plan. Governing MADARAII revision remains `7d5ef3d92c4e0982061d422208fdf913c55cc604`.

## Implementation baseline

The non-admitted candidate present after the original target contained a .NET 10 WPF shell, paragraph/run DOCX codec, Windows PDF module scaffolding and explicit storage boundary. It was retained as implementation baseline rather than discarded when `INBOX-0002` expanded the target.

Material deltas executed under this revision:

- replace the flat menu/toolbar UX with bounded WPF Ribbon organization;
- add structure-preserving Find/Replace/Replace All;
- extend direct formatting with highlight and bounded paragraph/cell fill/spacing/indent;
- extend compatibility scanner + FlowDocument reader/writer to simple editable tables;
- add table insertion and rectangular row/column operations;
- extend compatibility extraction for unsupported drawings/fields/footnotes with explicit preview/placeholder/reference text behavior where feasible;
- complete PDF/storage/build/install/tests/docs/evidence and verify the exact candidate.

## Implementation slices

| Slice | Outcome | Dependency | Producer evidence / terminal disposition |
|---|---|---|---|
| `S1` shell revision | first-party Ribbon, mode-aware groups, quick access, commands | revised target | implemented; exact candidate in `IMPLEMENTATION-0001` |
| `S2` editor operations | find/replace, highlight/fill/paragraph commands, simple table insertion/mutation | S1 | implemented; semantic fixtures passed in `EV-0001` |
| `S3` DOCX codec revision | simple-table/format admission, read/write, compatibility extraction and preservation | S2 | implemented; representative round-trip/fail-closed checks passed |
| `S4` PDF/storage | first-party PDF rendering, shutdown containment and explicit document writer | existing mechanisms + target | implemented; PDF render and normal-close process-exit verified on CI Windows environment |
| `S5` lifecycle | repository-clean build; per-machine install/uninstall/ownership manifest | S1–S4 | implemented; install/uninstall/residue flow passed on CI Windows environment |
| `S6` docs/verification | user/build/compatibility docs, automated static/fixture checks and Windows verification script | S1–S5 | established and exercised |
| `S7` fan-in | exact Product candidate, Implementation Result, typed verification, integration/state reconciliation and closure route | all | candidate `b2cafabba970245b7ccb24f6f01a3ca2e2ba9681` verified and admitted as `PRODUCT-0001` |

## Invariants

No third-party product/runtime dependency. No app-state/temp/cache writes. No silently lossy editable DOCX save path. Simple-table mutations remain rectangular. Unsupported drawing/field/reference semantics never enter editable-save authority. PDF stays read-only. User documents stay outside uninstall ownership. No background runtime component. Untested environment/format breadth remains explicit verification limitation rather than inferred conformance.

## Choice delegation

Private helper names and local source arrangement were delegated while preserving `PA-0001`. User-visible feature semantics, platform/dependency boundary, compatibility admission, persistence ownership, save durability trade-off, process-exit behavior, object/reference compatibility boundary and installation ownership remained fixed.

## Recovery and interruption

Repository version history preserves every implementation attempt. The terminal exact Product realization is bound by `PRODUCT-0001`; material Product changes create a new candidate rather than reopening this historical Plan silently.

## Verification contract and result

The Plan required typed verification rather than a scalar green label. `VERIFICATION-0001` records exact per-claim conclusions. Core tested claims conform; Windows 11 interactive/visual qualification and broad real-document compatibility remain bounded evidence gaps with explicit local procedures/reopen triggers.

## Terminal disposition

This Plan revision is fully executed for the Commissioned implementation package. It creates no implicit successor Work. Future feature expansion or repair requires fresh Commission/replanning against the current Product Baseline.

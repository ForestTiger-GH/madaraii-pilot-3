# Target HOW Reconciliation — HOW-E001-01

**Instruction:** MADARAII-24  
**Subject:** `TARGET-HOW-0001` revision 2, compatibility-view and search claims implicated by `JR1-A4/A5`  
**Target WHAT:** `TARGET-WHAT-0001` revision 2, unchanged  
**Product Baseline:** `b2cafabba970245b7ccb24f6f01a3ca2e2ba9681`  
**Authority:** act `A3` of `INPUT-DEV-E001-0001` as reconciled by `RECON-E001-0002`

## Assembly contract

Consumer jobs are implementation, verification, operation, and public compatibility description for MiniDoc 0.1. Scope is intentionally restricted to claims proven inconsistent with the admitted Product. No Scientific Knowledge reopening is required. The materiality threshold is whether a cold implementer/verifier could rely on a mechanism the Product does not actually provide.

## Result

A repaired Target HOW revision 3 is justified without changing Target WHAT:

1. Compatibility mode remains read-only and fail-closed. Current 0.1 mechanism is text extraction plus explicit markers/placeholders for graphics, notes, and fields. Relationship-backed raster/fallback rendering and resolved notes-body presentation are **not** current mechanisms and therefore must not be stated as implemented HOW.
2. Current UI search is case-insensitive. The reusable `TextSearch` mechanism may accept a case-sensitivity parameter internally, but no current UI toggle is part of Target HOW. Adding such a toggle would be a separate Product choice, not a documentation repair.
3. Public `docs/COMPATIBILITY.md` is a derived Product-facing projection and must be refreshed from the repaired owner: the compatibility viewer preserves unsupported source and surfaces text/markers; it does not claim actual package-preview rendering in 0.1.

All other Target HOW mechanisms, choices, WHAT trace, and reopen triggers remain unchanged at this step. Product implementation repairs for `JR1-A1/A2/A3/A6` remain separately routed Work and will later require their own HOW/Product reconciliation if their accepted mechanisms materially alter this owner.

## Choice states

- marker/text compatibility presentation: `FIXED` for current 0.1;
- richer image/note preview extraction: outside current committed mechanism; may be reconsidered only through a later target change;
- case-insensitive UI search: `FIXED` for current 0.1;
- case-sensitive UI control: outside current committed mechanism.

No blocking choice remains in this bounded HOW slice.

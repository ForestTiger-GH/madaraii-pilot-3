# Target WHAT Reconciliation — WHAT-E001-02

**Instruction:** MADARAII-17  
**Subject:** `TARGET-WHAT-0001` revision 2, compatibility-mode graphic/note presentation slice  
**Current Product:** `PRODUCT-0001 / 4a7c1d5b2bea939ebd508dcce19c4e93f58149cf`  
**Current Target HOW:** revision 4  
**Trigger:** `JR2-A3` as admitted by `RECON-E001-0006`

## Assembly boundary

This Work is limited to the stale compatibility-presentation commitment language. Product identity, editable DOCX subset, PDF boundary, lifecycle, installation, save durability and unrelated target commitments are outside the mutation scope.

Current owner evidence is decisive enough for this bounded reconciliation: Product implementation, Target HOW revision 4 and `docs/COMPATIBILITY.md` consistently implement/state main-story text extraction plus explicit unsupported-semantics markers/placeholders without dereferencing package media or notes parts. `HOW-E001-01` previously accepted that as the intended current 0.1 mechanism and explicitly left richer preview/note presentation for a later target change.

## Commitment disposition

Target WHAT revision 2 used broader conditional language suggesting that safely related raster/fallback previews and note-body text could be presented in compatibility mode. Even where phrased as `may`, those statements create an avoidable consumer ambiguity because the current Product-facing target otherwise describes the release boundary concretely.

Revision 3 therefore clarifies the **current 0.1 Product commitment** rather than adding Product capability:

- compatibility mode guarantees best-effort safely extractable main-story text and explicit markers/placeholders for unsupported graphics/references/fields;
- it does not commit MiniDoc 0.1 to dereference package media relationships for raster/chart/SmartArt/shape previews;
- it does not commit MiniDoc 0.1 to dereference footnote/endnote parts to append note-body text;
- safely available cached/result text in the main story may remain visible, but fields/TOC are never recalculated;
- richer relationship-backed preview or note-body presentation is a future target change, not latent implementation freedom inside current 0.1.

This matches the admitted Product and public compatibility projection and removes the current owner ambiguity without Product mutation.

## Authority and semantic effect

The human Commission authorizes integration of useful Jester-derived corrections in this epoch. This revision does not reinterpret external obligation, remove an observed working capability, or invent a new mechanism. It narrows stale optional/conditional wording to the already accepted 0.1 boundary established by prior Target HOW reconciliation.

No blocking Target Decision remains in this slice. Target WHAT revision 3 is ready for Target HOW handoff. `JR2-A1/A2/A4` remain separate downstream concerns and are not silently resolved by this WHAT change.
